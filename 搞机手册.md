# NROS / 鲲鹏 搞机手册（速查）

局域网管理地址以 **192.168.66.1** 为例，请按你实际 LAN IP 替换。

## 1. 开启 SSH

LuCI：**系统 → 安全**（或直接打开）

- http://192.168.66.1/cgi-bin/luci/admin/system/security/

## 2. 主题切换

LuCI：**系统 → 系统**（主题相关选项通常在此）

- http://192.168.66.1/cgi-bin/luci/admin/system/system

## 3. 插件安装助手（第三方）

- https://github.com/isJackeroo/NROS_plugins

## 4. App Center 页面文件（固件内路径）

路由器上：

- `/usr/lib/lua/luci/view/nradio_appcenter/appcenter.htm`

## 5. 官方 NROS / OpenWrt 软件源（`distfeeds` 参考）

以下为常见 `src/gz` 写法，**整行复制到 `/etc/opkg/distfeeds.conf` 或对照厂商固件**即可。

> 说明：`21.02-SNAPSHOT` 在官方镜像上可能随时间 **404**；离线或失效时可配合本仓库的 `sync-opkg-offline-from-pc.ps1` 用 PC 拉取 `Packages.gz` 再喂给路由器，或向厂商索取当前可用源地址。

```
src/gz openwrt_core https://downloads.openwrt.org/releases/21.02-SNAPSHOT/targets/mediatek/mt7987/packages
src/gz openwrt_base https://downloads.openwrt.org/releases/21.02-SNAPSHOT/packages/aarch64_cortex-a53/base
src/gz openwrt_mtk_openwrt_feed https://downloads.openwrt.org/releases/21.02-SNAPSHOT/packages/aarch64_cortex-a53/mtk_openwrt_feed
src/gz openwrt_packages https://downloads.openwrt.org/releases/21.02-SNAPSHOT/packages/aarch64_cortex-a53/packages
src/gz openwrt_routing https://downloads.openwrt.org/releases/21.02-SNAPSHOT/packages/aarch64_cortex-a53/routing
```

其中 **`openwrt_mtk_openwrt_feed`** 一行若你手头文档只写到 `.../aarch64_cortex-a53/`，请补全为末尾 **`mtk_openwrt_feed`**（与常见 distfeeds 命名一致）。

## 与本仓库脚本的关系

- **一键美化 / iStore 化**：`kunpeng-istore.sh`，Windows 下发：`ssh-deploy-kunpeng.ps1`
- **路由器上不了官方源时**：`sync-opkg-offline-from-pc.ps1`（用 PC 下载索引后 `file://` 临时替换，详见脚本内说明）
- **恢复原厂 distfeeds / customfeeds 备份**：`sync-opkg-offline-from-pc.ps1 -Restore`
